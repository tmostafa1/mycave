#removing duplicates from a list
def rem_dup(p):
    p=list(p)
    m=sorted(list(dict.fromkeys(p)))#turn the list to a dictionary first because dictionary keys donot have duplicates and then turn it to a list again
    print(m)
p=(1,3,4,9,9,5,5,6,6,7,8)
rem_dup(p)
#output:
[1, 3, 4, 5, 6, 7, 8, 9]
